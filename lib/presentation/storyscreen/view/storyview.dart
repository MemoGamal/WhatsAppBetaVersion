import 'package:flutter/material.dart';

class Storyview extends StatelessWidget {
  const Storyview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Story View"),
    );
  }
}
